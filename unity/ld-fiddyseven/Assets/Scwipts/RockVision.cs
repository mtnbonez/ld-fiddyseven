using UnityEngine;

public class RockVision : MonoBehaviour
{

    MeshRenderer RockMesh;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
